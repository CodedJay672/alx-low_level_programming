#include "main.h"

/**
 * mul - multiplies two numbers
 * @a: first argument
 * @b: second argument
 * Return: Returns the product of a and b
 */

int mul(int a, int b)
{
	int prod = a * b;
	return (prod);
}
